import {
  ConfidenceIndicator,
  type ConfidenceIndicatorProps,
} from '@/components/ui/ConfidenceIndicator';

export function AIConfidenceBadge(props: ConfidenceIndicatorProps) {
  return <ConfidenceIndicator {...props} />;
}
